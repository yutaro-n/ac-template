#![allow(unused_imports)]
use std::collections::*; //{Vec, VecDeque, LinkedList, HashMap, BTreeMap, HashSet, BTreeSet, BinaryHeap};
use itertools::Itertools;
use proconio::{
    input,
    marker::* //{Chars, Bytes, Isize1, Usize1}
};

fn main() {
    input! {

    }
}
